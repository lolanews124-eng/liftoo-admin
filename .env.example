# Local backend API (used at build time for Vite env injection)
VITE_API_URL=http://localhost:5000/api/v1

# On Vercel, set VITE_API_URL in Project Settings → Environment Variables
# to your production API URL, e.g. https://api.liftoo.in/api/v1
